# 扬州高露洁Koch机视觉检测系统 — 全面问题分析报告

> **日期:** 2026-07-07  
> **范围:** 上下端面工位 / 侧面检测工位 / 程序并发风险 / 防呆逻辑缺失  
> **数据来源:** log.txt (夜班日志)、EndFaceStationProcessor.cs、SideStationProcessor.cs、CameraTriggerManager.cs、MotionControlManager.cs、MainFrm.cs

---

## 一、上下端面工位 — "缺少"问题分析

### 1.1 问题现象
- 日志统计："缺少" 字样出现约 156 次（60板测试数据片段）
- 夜班6000+板，推算缺少出现约 700+次（约1/10板）
- 日期码相机记录的真正缺件 NG 照片不到 10板

### 1.2 日志证据

正常的批次:    [EndFace] 完成 P=12 OK=12 NG=0                        | 耗时≈500ms    ← 约85%
部分缺失:      [EndFace] 完成 P=11 OK=11 NG=1 | 上端面:缺少1 下端面:缺少1 | 耗时≈800ms    ← 约8%
灾难性缺失:    [EndFace] 完成 P=1  OK=1  NG=11| 上端面:缺少11 下端面:缺少11| 耗时≈50ms     ← 约7%

特征：P=1, NG=11 这种灾难性批次耗时仅50ms，几乎没有做实际推理。

### 1.3 根因分析 — 缺少是假阳性，不是真正的缺件

问题出在 EndFaceStationProcessor.cs 的 EnqueueImage() 方法（第140-177行）：

新批次检测条件: productId - _firstBatchProductId > _pCount * 3 (=36)

关键BUG:
1. 当触发新批次检测时，强制flush不完整批次（可能只有1-2张图）
2. 然后丢弃队列中所有剩余图片: while (_upperQueue.TryDequeue(out var _)) _upperCount--;
3. _firstBatchProductId 重置为新ProductId，但下一张图的ProductId又可能跳过36+

为什么阈值36太敏感？
ProductId 是全局计数器，8台相机共享。一个12盒产品周期内:
- 正面 Cam1+Cam2: 24次增量
- 背面 Cam5+Cam6: 24次增量
- 端面 Cam3+Cam4: 24次增量
- 侧面 Cam7+Cam8: 24次增量
合计约96次增量 >> 阈值36 → 批次检测几乎在每个周期都错误触发

**结论：日志中700+次"缺少"至少有95%是假阳性。**

### 1.4 修复方案

推荐：将 ProductId 跳变检测改为基于入队时间间隔的检测（3秒无新图 -> 视为新批次）

---

## 二、侧面检测工位 — 三大问题分析

### 2.1 12P系统经常拍不到12张

日志证据：
[Side] 完成 P=12 OK=7 NG=5 | 左侧面:缺少5 右侧面:缺少5
[Side] 完成 P=12 OK=0 NG=12| 左侧面:缺少12 右侧面:缺少12
缺少从1张到12张不等，左右同时缺少相同数量非常普遍。

根因1：相机触发由ZMC BASIC程序控制
MainFrm.cs 第450行: ExternalTriggerEnabled = true
PC端不再输出OUT脉冲。如果BASIC程序拍照位置点不合理，触发数 < 24。

根因2：ProcessStream 10秒超时退出，已处理 < 24张。

根因3（严重BUG）：同一周期结果被重复记录3-5次
[13:45:01.389] [Side] 完成 P=12 OK=9 NG=3 | ...
[13:45:01.389] [Side] 完成 P=12 OK=9 NG=3 | ...  ← 完全相同！重复5次

原因：侧面排队轮询机制的 FinalizeResults 被多个触发源同时调用。
OnCameraTriggered 中的排队逻辑和 finally 块的收尾逻辑竞争。

### 2.2 渲染图问题

"?" 显示：EmitPartial() 中实时推送时，未完成位置显示"?"。
OK图片存错目录：SaveImages() 中左右图数量不一致时索引映射错误。

### 2.3 拍照不及时 — 898ms延迟

正常周期:
IN13 -> Task.Run 4ms -> StartMotion开始

异常周期:
IN13 -> Task.Run 2ms -> StartMotion延迟 898ms !!!

根因：上一周期 FinalizeResults Stage 2 后台执行 BuildDisplayImages
（GDI+渲染24张图，约720-1440ms）+ SaveImages（文件I/O），
占用了线程池，导致下一周期 Task.Run 排队等待。

---

## 三、程序并发风险分析

### 3.1 并发统计

高峰期并发：约 25-30 个线程/Task 同时运行

| 类别 | 数量 | 线程/任务 |
|------|------|----------|
| CameraTriggerManager | 3线程 | Monitor(Highest+CPU), PulseOut(AboveNormal), Stats |
| EndFace ProcessLoop | 1线程 | AboveNormal |
| Front/Back/Side 推理 | 10-15个Task | 每批次并行推理 |
| Logger | 1线程 | LongRunning 写文件 |
| ImageSaver | 2线程 | Bitmap + byte[] |
| UI | 1线程 | WinForms消息泵 |
| 相机SDK回调 | 8线程 | DaHuaSDK.OnImage |
| 心跳/轮询 | 3个 | 150ms/500ms Timer |

### 3.2 主要风险

🔴 风险1：线程池饥饿 - StartMotion 延迟898ms，可能导致跟不上产线节拍
🔴 风险2：GPU竞争 - 端面batch推理 vs 侧面实时推理，延迟波动320~2274ms
🟡 风险3：GDI+资源竞争 - RenderSideImage 多Task同时操作GDI+，可能死锁
🟡 风险4：UI BeginInvoke过载 - 每次UpdateStatistics更新12个Label
🟡 风险5：Logger队列积压 - Debug日志过多，异常崩溃时丢失

---

## 四、缺失的防呆逻辑清单

### EndFaceStationProcessor
🔴 上下端面图像数量一致性校验
🔴 连续异常批次检测（连续N批缺少 -> 告警灯/PLC）
🔴 相机长时间无图像告警
🟡 批次超时PLC通知
🟡 队列丢弃数量记录

### SideStationProcessor
🔴 FinalizeResults 防重入（添加 _finalizeInProgress 标志位）
🔴 左右图像数量一致性校验
🔴 连续"缺少"PLC告警
🟡 运动轴3次重发失败告警
🟡 ProcessStream 首图超时告警
🟡 results索引越界保护

### CameraTriggerManager
🟡 MonitorLoop 频率 <500Hz 告警（CPU过载）
🟡 单端口触发频率异常检测（传感器故障）

### 全局
🔴 相机图像超时告警
🟡 内存使用率监控（>4GB告警）
🟡 线程池可用线程监控（<4告警）
🟢 磁盘空间监控

---

## 五、优化建议

### 紧急修复（本周）

1. 端面工位 EnqueueImage 批次检测改时间基准

方案：用入队时间间隔替代 ProductId 跳变

    // EndFaceStationProcessor.cs EnqueueImage() 修改
    private const int MaxBatchIntervalMs = 3000;
    if (DateTime.Now - _lastEnqueueTime).TotalMilliseconds > MaxBatchIntervalMs
    {
        // flush 当前不完整批次 + 清空残留
        int partialP = Math.Min(_upperCount, _lowerCount);
        if (partialP > 0) { var ul = DequeueBatchN(_upperQueue, ref _upperCount, partialP); ... }
        while (_upperQueue.TryDequeue(out var _)) ;
        while (_lowerQueue.TryDequeue(out var _)) ;
        _upperCount = 0; _lowerCount = 0;
    }


2. 侧面 FinalizeResults 防重入

    // SideStationProcessor.cs 添加
    private volatile int _finalizeInProgress = 0;
    private void FinalizeResults()
    {
        if (Interlocked.CompareExchange(ref _finalizeInProgress, 1, 0) != 0)
        {
            Logger.Warning("[Side] FinalizeResults 重入被阻止");
            return;
        }
        try { /* ... 原有逻辑 ... */ }
        finally { Interlocked.Exchange(ref _finalizeInProgress, 0); }
    }



3. 侧面渲染Task改用低优先级专用线程（避免阻塞检测Task）

4. 检查ZMC BASIC程序触发配置（24个拍照位置点是否正确）

### 架构优化（本月）

1. 侧面工位使用专用后台线程替代 Task.Run
2. 渲染按需惰性化（存原图+AI结果JSON，需要时渲染）
3. GPU推理队列化（BlockingCollection排队，避免多Task竞争GPU）
4. 全局健康监控器（线程池/内存/连续异常检测）

---

## 六、总结表

| 问题 | 根因 | 严重度 | 修复方向 |
|------|------|--------|---------|
| 端面缺少700+次 | ProductId阈值P*3太小(全局计数器快速跳过) | 🔴严重 | 时间基准批次检测 |
| 侧面缺少700+次 | ZMC触发不完整 + FinalizeResults重复调用 | 🔴严重 | 检查ZMC+防重入 |
| 拍照不及时(898ms) | 线程池被渲染/存图Task占满 | 🔴严重 | 专用线程+惰性渲染 |
| 存图含?/OK图片 | ClearBatch与Stage2渲染竞争 | 🟡中等 | 渲染数据copy独立 |
| 处理过多(25-30并发) | Task.Run滥用 + 无GPU推理队列 | 🟡中等 | GPU推理队列化 |
| 缺少防呆 | 多处缺少异常检测和告警 | 🟡中等 | 按清单补充10+项 |

### 修复优先级

| 优先级 | 修复项 | 预计工作量 |
|--------|--------|-----------|
| P0 | 端面批次检测 + 侧面防重入 + 渲染Task线程优化 | 1.5天 |
| P1 | 检查ZMC BASIC + 补充7项主要防呆 | 2天 |
| P2 | GPU推理队列化 + 健康监控 + 渲染惰性化 | 5天 |

### 三个核心结论

1. 端面"缺少"95%是假阳性，由ProductId批次检测逻辑错误导致，需要立即修复
2. 侧面"缺少"由两个原因叠加：ZMC触发不完整 + FinalizeResults被重复调用
3. 898ms延迟是线程池饥饿的直接后果，需要将渲染/存图从线程池中剥离
